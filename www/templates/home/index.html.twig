{% extends 'base.html.twig' %}

{% block title %}Accueil - Hoomy
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<h1 class="text-center mb-4 text-white">Choisissez votre vibe 🎶</h1>

		<div class="row g-4">
			{% for vibe in vibes %}
				<div class="col-md-4 col-sm-6">
					<div class="card bg-dark text-white shadow-lg border-0">
						<img src="{{ asset('images/vibe_placeholder.jpg') }}" class="card-img-top" alt="Vibe Image">
						<div class="card-body">
							<h5 class="card-title">{{ vibe.label }}</h5>
							<p class="card-text">Énergie:
								{{ vibe.energy }}</p>
							<p class="card-text">Stress:
								{{ vibe.stress }}</p>
							<p class="card-text">Motivation:
								{{ vibe.motivation }}</p>
							<a href="{{ path('vibe_detail', {id: vibe.id}) }}" class="btn btn-success">Détails</a>
						</div>
					</div>
				</div>
			{% else %}
				<p class="text-center text-white">Aucune vibe disponible pour l'instant.</p>
			{% endfor %}
		</div>
	</div>
{% endblock %}
